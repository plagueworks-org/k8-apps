apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: face-verification

resources:
  - es.yaml
  - helmrelease.yaml

generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
- name: compreface-values
  namespace: face-verification
  files:
  - values.yaml
