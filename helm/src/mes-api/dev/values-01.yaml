
# App name
name: mes-api

# Namespace
namespace:
  create: false

# Replica
replicaCount: 1

# Image
image:
  repository: ghcr.io/mervinhemaraju/mauritius-emergency-service-api
  tag: dev20260323-5e67216
  pullPolicy: Always

# Container port
port: 8081

# Container envs
env:
  - name: UWSGI_PROTOCOL
    value: "http"
  - name: MAIL_USERNAME
    value: "mervinhemaraju@gmail.com"
  - name: MAIL_PASSWORD
    value: ""

# ConfigMap for data files
# configMap:
#   enabled: true
#   filesGlob: "data/v1/*.json"

# Volumes and mounts
volumes:
  - name: data-contacts
    configMap:
      name: mes-api-contacts-configmap
  - name: data-cyclone
    configMap:
      name: mes-api-cyclone-configmap
  - name: data-torrentialrain
    configMap:
      name: mes-api-torrentialrain-configmap
  - name: data-tsunami
    configMap:
      name: mes-api-tsunami-configmap

volumeMounts:
  - name: data-contacts
    mountPath: /app/data/v1/contacts
    readOnly: true
  - name: data-cyclone
    mountPath: /app/data/v1/cyclone
    readOnly: true
  - name: data-torrentialrain
    mountPath: /app/data/v1/torrentialrain
    readOnly: true
  - name: data-tsunami
    mountPath: /app/data/v1/tsunami
    readOnly: true

# Health checks
healthChecks:
  enabled: true
  livenessProbe:
    httpGet:
      path: /health
      port: 8081
    initialDelaySeconds: 15
    periodSeconds: 20
    timeoutSeconds: 5
    failureThreshold: 3
  
  readinessProbe:
    httpGet:
      path: /health
      port: 8081
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3

# Resources
resources:
  requests:
    cpu: "50m"
    memory: "128Mi"
  limits:
    cpu: "100m"
    memory: "256Mi"

# Service
service:
  enabled: true
  type: ClusterIP
  port: 80
  targetPort: 8081

# Ingress
ingress:
  enabled: true
  hosts:
    - host: staging-mesapi.mervinhemaraju.com
    - host: staging-mesapi.plagueworks.org
  entryPoints:
    - websecure
  tls:
    enabled: true
    certResolver: le
  externalDns:
    enabled: true
    target: traefik-dev.plagueworks.org
    cloudflareProxied: "true"
    cloudflareComment: "MES API managed by ExternalDns in oke-helios-dev-apps"

# Applies node affinity
nodeAffinity:
  enabled: true
  required:
    - key: node-pool
      operator: In
      values:
        - default-pool