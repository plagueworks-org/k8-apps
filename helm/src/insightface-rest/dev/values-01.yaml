# App name
name: insightface-rest

# Namespace
namespace:
  create: false
  name: face-verification

# Replica
replicaCount: 1

# Image
image:
  repository: ghcr.io/plagueworks-org/insightface-rest
  tag: latest
  pullPolicy: Always

# Container port
port: 18080

# Container envs
env:
  - name: PORT
    value: "18080"
  - name: INFERENCE_BACKEND
    value: "onnx" # default to onnx for CPU/ARM64 inference
  - name: LOG_LEVEL
    value: "INFO"
  - name: NUM_WORKERS
    value: "1"

# Deployment
deployment:
  imagePullSecrets:
    - name: ghcr-pull-secret

# Health checks
healthChecks:
  enabled: true
  liveness:
    enabled: true
    path: /info
    port: 18080
    initialDelaySeconds: 30
    periodSeconds: 20
    timeoutSeconds: 5
    failureThreshold: 3
  readiness:
    enabled: true
    path: /info
    port: 18080
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3

# Resources
resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "300m"
    memory: "1024Mi"

# Service
service:
  enabled: true
  type: ClusterIP
  port: 80
  targetPort: 18080

# No ingress — internal only
ingress:
  enabled: false

# Applies node affinity
nodeAffinity:
  enabled: true
  required:
    - key: node-pool
      operator: In
      values:
        - default-pool
