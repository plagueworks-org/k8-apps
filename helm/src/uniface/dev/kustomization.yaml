apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: face-verification

resources:
  - namespace.yaml
  - helmrelease.yaml

generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
- name: uniface-values
  namespace: face-verification
  files:
  - values-01.yaml
