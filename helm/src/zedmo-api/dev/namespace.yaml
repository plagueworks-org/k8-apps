apiVersion: v1
kind: Namespace
metadata:
  name: zedmoapi
  labels:
    function: application
    category: api
    # Provisions ghcr-pull-secret into this namespace via the ghcr-pull-secret
    # ClusterExternalSecret. Required: the image is private.
    es: ghcr
